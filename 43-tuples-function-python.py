#Tuple Method Or Function In Python

tup = (1,2,4,3,5,2,3,2,2,)

# tup[0] = 10 #Tuples are immutable, so this will raise an error


#.index(element)
print(tup.index(4))  #return the idex of first occurrence # return 2

#.count(element)
print(tup.count(2)) #Count the Total Number of Occurrence occur . 
